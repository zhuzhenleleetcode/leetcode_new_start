class Solution {
    public int titleToNumber(String s) {
        int res=0;
        for(int i=0; i<s.length(); i++){
            char cur = s.charAt(i);
            res=res*26+(cur-'A')+1;
        }
        return res;
    }
}
